//No functions calls, bindings or anything that requires computation,
//property names have to be sorrounded by double quotes,
//EXAMPLE
//A journal entry might look like this when represented as JSON data:
{
"squirrel": false,
"events": ["work", "touched tree", "pizza", "running"]
}
